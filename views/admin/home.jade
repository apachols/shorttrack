extends ../layout

block content
  .row
    .col-xs-12
      a(href="/genders") Manage Genders
      .panel.panel-default
        .panel-heading Users
        .panel-body
          table.table.table-striped
            thead
              tr
                th Username
                th Salt
            tbody
              each user in users
                tr
                  td: a(href="/admin/#{user.username}") #{user.username}
                  td #{user.salt}
