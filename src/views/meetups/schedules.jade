extends ../layout

block content
  table.table.table-striped.table-bordered.table-condensed
    thead
      tr
        th Round
        th User A
        th User 1
        th Score

    tbody
      for date in matches
        tr
          td #{date.round}
          td: a(href="/meetup/#{meetup.name}/schedule/#{date.user1.email}") #{date.user1.email}
          td: a(href="/meetup/#{meetup.name}/schedule/#{date.user2.email}") #{date.user2.email}
          td #{date.score}

  pre= inspect(matches)


